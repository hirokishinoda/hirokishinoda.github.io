version: '3'
services:
  app:
    build: .
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/usr/src/app
    command: sh -c 'cd portfolio && yarn start'
    ports:
      - 8000:3000
    tty: true